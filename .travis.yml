language: python
python:
  - "2.7"
  - "3.6"

install:
  - pip install Cython
  - pip install netcdf4
  - pip install matplotlib

script:
  - locate netcdf4
  - python setup.py build_ext --inplace
  - py.test -s -v tests

